<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @authors.each do |author| %>
  <tr>
    <td><%= author.first_name %></td>
    <td><%= author.last_name %></td>
    <td><%= link_to 'Show', author %></td>
    <td><%= link_to 'Edit', edit_author_path(author) %></td>
    <!-- <td><%= link_to 'Delete', author, :method => :destroy, :data => { :confirm => 'Are you sure?' } %></td>  -->
    <td><%= link_to 'Delete', author, {:action => :destroy, :id => author}, :confirm => 'Are you sure?' %></td> 
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Author', new_author_path %>
