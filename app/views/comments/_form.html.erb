<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p><label for="comment_book">Book</label><br/>
  <%= collection_select :comment, :book_id, @books, :id, :title %></p>  
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id, :value => current_user.id, :readonly => true %>
  </div>  
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text %>
  </div>

  (Test - Book id: <%= session[:bookId] %>)

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
